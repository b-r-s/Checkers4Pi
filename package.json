{
  "name": "ai-checkers-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run --workspace=packages/web dev",
    "build:web": "npm run --workspace=packages/web build",
    "install:all": "npm install"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}